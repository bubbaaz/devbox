# starship.toml configuration

format = """
$username\
$hostname\
$directory\
$git_branch\
$git_state\
$git_status\
$nix_shell\
$rust\
$package\
$python\
$lua\
$nodejs\
$memory_usage\
$battery\
$time\
$cmd_duration\
$line_break\
$character"""

[directory]
style = "bold blue"
truncate_to_repo = true
truncation_length = 3
fish_style_pwd_dir_length = 1

[character]
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"

[git_branch]
symbol = " "
style = "bold purple"

[git_status]
style = "bold red"

[nix_shell]
symbol = " "
format = "via [$symbol$state]($style) "

[rust]
symbol = "🦀 "
format = "via [$symbol($version )]($style)"

[python]
symbol = " "
format = "via [$symbol($version )(($virtualenv) )]($style)"

[nodejs]
symbol = " "
format = "via [$symbol($version )]($style)"

[lua]
symbol = " "
format = "via [$symbol($version )]($style)"

[cmd_duration]
min_time = 500
format = "took [$duration]($style) "

[battery]
full_symbol = "🔋 "
charging_symbol = "⚡️ "
discharging_symbol = "💀 "
format = "[$symbol$percentage]($style) "
disabled = false

# Show battery even if decent charge (for visibility)
[[battery.display]]
threshold = 100
style = "bold green"

[memory_usage]
disabled = false
threshold = -1
symbol = "🐏 "
format = "via $symbol[${ram}( | ${swap})]($style) "
style = "bold dimmed white"

[time]
disabled = false
format = "at [$time]($style) "
time_format = "%T"
style = "bold yellow"
